namespace WolflineSquadTTT.Models.Enums
{
    // Events a webhook can subscribe to. Extend as more become available.
    public enum WebhookEvent
    {
        MarketListingCreated = 0,
        PollCreated = 1
    }
}
