namespace WolflineSquadTTT.Models.Enums
{
    public enum PollType
    {
        Favorite = 0,     // select one
        Tierlist = 1,     // ordered list
        MultiSelect = 2   // select multiple
    }
}
